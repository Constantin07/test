---

keepCRDs: false   # For clean uninstall

controller:
  replicas: 1

dashboard:
  enabled: true

  ingress:
    enabled: false
    ingressClassName: kong
    hosts:
      - argorollouts.internal

  httproute:
    enabled: true
    parentRefs:
      - name: kong
        namespace: kong
    hostnames:
      - argorollouts.internal
