---

- hosts: jenkins_agents
  become: yes
  become_user: root
  any_errors_fatal: true

  roles:
    - resolv
    - common
    - chronyd
    - docker
    - jenkins_agent
    - gnupg2
    - prometheus/node_exporter
